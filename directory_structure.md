# プロジェクトのフォルダ構成（ディレクトリ構造）詳細

Next.js（App Router）を用いて、今回設計した「マルチゲーム対応アーキテクチャ」と「Supabase連携」を最も美しく実装するためのフォルダ構成案です。
実際に `npx create-next-app` でプロジェクトを作った後の `src/` フォルダの中身を想定しています。

---

```text
src/
 ├─ app/                        # 📍 Next.js のルーティング（URLと画面の紐付け）
 │   ├─ layout.tsx              # 全体レイアウト（フォント設定、共通ヘッダーなど）
 │   ├─ page.tsx                # ポータルトップページ（ゲーム一覧、ルーム作成ボタンなど）
 │   ├─ login/                  
 │   │   └─ page.tsx            # Googleログイン画面
 │   └─ room/                   
 │       └─ [roomId]/           # 個別ルーム画面（URL: /room/1234 等）
 │           └─ page.tsx        # 選択されたゲームコンポーネントを呼び出す司令塔
 │
 ├─ components/                 # 🧩 全体で共通利用するUIコンポーネント
 │   ├─ ui/                     # ボタン、モーダル等の汎用パーツ (shadcn/ui等を想定)
 │   └─ shared/                 # アバター表示、汎用チャット欄、参加者リストなど
 │
 ├─ games/                      # 🎮 ★マルチゲーム対応の中核（プラグインフォルダ）
 │   ├─ core/                   # 共通のインターフェースや型定義
 │   │   ├─ types.ts            # 先ほどの `typescript_types.md` の内容
 │   │   └─ GameWrapper.tsx     # ゲーム画面を包む共通の枠組み
 │   │
 │   ├─ fake-artist/            # エセ芸術家の固有ロジック・コンポーネント群
 │   │   ├─ index.tsx           # ゲームのエントリーポイント（大元）
 │   │   ├─ components/         # エセ固有のUI（キャンバス、お題入力フォーム等）
 │   │   └─ hooks/              # エセ固有のロジック (useFakeArtist 等)
 │   │
 │   └─ registry.ts             # 新しいゲームを追加する際の「登録所」
 │
 ├─ lib/                        # 🔧 共通ライブラリ・設定ファイル
 │   ├─ supabase/               # Supabaseのクライアント設定
 │   │   ├─ client.ts           # フロントエンド（ブラウザ側）用
 │   │   └─ server.ts           # サーバー側（Server Actions用）
 │   └─ utils.ts                # 便利関数（Tailwindのクラス結合など）
 │
 └─ hooks/                      # 🎣 全体で共通利用するカスタムフック
     ├─ useRoomSubscription.ts  # roomsテーブルを監視してStateを返すフック
     └─ useGameEvents.ts        # game_eventsテーブルを監視してEventを返すフック
```

---

## 設計のポイントと役割

### 1. `app/room/[roomId]/page.tsx` の役割（司令塔）
このページは**「今この部屋はどのゲーム(game_type)を選んでいるか？」**をデータベースから取得し、その結果に応じて `games/` フォルダから該当するゲームのコンポーネントを呼び出します。
そのため、このファイル自体にはエセ芸術家の固有ルールは一切書きません。

### 2. `games/` フォルダの役割（プラグイン）
各ゲームの中身はこのフォルダ内に完全に閉じ込めます。
今後「人狼ゲーム」を追加したくなったら、`games/werewolf/` というフォルダを作り、必要なコンポーネントをその中に書くだけで済みます。ポータル側（`app/`）のコードをぐちゃぐちゃに汚さずに拡張できます。

### 3. `hooks/` フォルダの役割（通信の共通化）
先ほどディスカッションした「2つの監視（StateとEvent）」を行うロジックは、全ゲームで共通です。
そのため `useRoomSubscription` や `useGameEvents` というカスタムフックにまとめておくことで、どのゲームを作る時でもこのフックを使い回すだけでリアルタイム通信が完成します。

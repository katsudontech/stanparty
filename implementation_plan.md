# ボードゲームポータル＆「エセ芸術家ニューヨークへ行く」 総合仕様書

本ドキュメントは、Next.js (Vercel) と Supabase を用いたサーバーレス環境でのボードゲームポータル、および第一弾ゲーム「エセ芸術家ニューヨークへ行く」の包括的な設計・仕様書です。

---

## 1. 全体アーキテクチャ方針

- **サーバーレス・クライアント主導設計**: 常駐サーバー（Node.js等）を持たないVercel環境に最適化するため、ゲームの進行は「プレイヤーのアクションをトリガーとしたDB（Supabase）の更新」によって管理します。
- **ドメイン戦略**: SEOとログイン状態の共有を最適化するため、ゲームごとに別ドメインを分けず、`https://example.com/games/fake-artist` のように**同一ドメインのディレクトリ分割**で構成します。
- **デザインテーマ**: 「ポップで賑やか」をテーマに、Tailwind CSS と Framer Motion を活用したリッチでアニメーション豊富なUIを構築します。

---

## 2. フロントエンド設計（マルチゲーム拡張対応）

将来的なゲームの追加を容易にするため、各ゲームを「プラグイン」のように独立して管理するディレクトリ構成とします。

```text
src/
 ├─ app/                 
 │   ├─ (portal)/        # トップページ、ログイン画面
 │   └─ room/[id]/       # ルーム画面（選択されたゲームを動的読み込み）
 ├─ components/          # 共通UI（ボタン、ヘッダー、汎用チャットなど）
 └─ games/               
     ├─ core/            # 全ゲーム共通のインターフェース・型定義
     ├─ fake-artist/     # ★エセ芸術家固有のコンポーネント・ロジック
     ├─ werewolf/        # （将来追加）人狼ゲーム
     └─ index.ts         # 各ゲームの登録・ルーティング設定
```

### リアルタイム通信（Supabase Realtime）の購読設計
フロントエンドは以下の**2つの監視（リッスン）のみ**で全ゲームの状態を同期します。
1. **`rooms` テーブルの UPDATE 監視** (`id=eq.自分のルームID`)
   - 目的: 現在のターン、参加者の増減、ゲームの進行フェーズなど「最新の状態（State）」の同期。
2. **`game_events` テーブルの INSERT 監視** (`room_id=eq.自分のルームID`)
   - 目的: 線の描画、チャットなど、積み重なっていく「アクションの履歴（Event）」の同期。

---

## 3. データベース設計（Supabase / PostgreSQL）

様々なルールのゲームに対応しつつ、通信量を最小限に抑える「ハイブリッド・イベントソーシング設計」を採用します。

| テーブル名 | カラム名 | データ型 | 役割・説明 |
| :--- | :--- | :--- | :--- |
| **`users`** | id, name, avatar | 汎用 | 全ユーザーの基本情報（Google認証と連携） |
| **`rooms`** | id, host_id | 汎用 | ルームの固有ID、作成者ID |
| | game_type | Text | 遊んでいるゲーム（例: `fake-artist`） |
| | status | Text | 'waiting', 'playing', 'finished' |
| | **players** | JSONB | 【共通】参加者情報（ユーザーID、色、役職、スコアなど） |
| | **game_state** | JSONB | 【状態】現在のターン、現在のお題など、ゲーム固有の「今の状況」。ターンが切り替わる時はこのJSONBを直接UPDATEする。 |
| **`game_events`**| id, room_id | 汎用 | 【履歴】各ゲームのアクション履歴を逃がすテーブル |
| | event_type | Text | イベントの種類（例: `draw_line`, `chat_message`） |
| | **payload** | JSONB | 線を描く座標などの詳細データ。1アクションごとにINSERTされるため、Realtime通信の負荷が極めて軽い。 |

---

## 4. 総合サイト（ポータル）の機能仕様

- **認証・ユーザー管理 (Google認証)**
  - Supabase Authの「Google Provider」を利用。パスワードレスでログインし、Googleアカウントからアイコンや名前を自動取得します。
- **ロビー・ルーム機能**
  - 新規ルームの作成、および招待用URLの発行機能。
  - ルーム参加者の待機室（ロビー）機能。ホストが強制的にターンを進めたりプレイヤーをキックできる権限を持ちます（サーバーレスでの進行停止を防ぐため）。

---

## 5. 「エセ芸術家ニューヨークへ行く」固有仕様

第一弾ゲームの進行ロジックです。

- **役職と「お題」の決定フェーズ**
  - ゲーム開始時、1名を「出題者」、1名を「エセ芸術家」、残りを「芸術家」にランダム割り当て。
  - 出題者が「お題のジャンル」と「お題」を入力。
  - エセ芸術家以外のプレイヤーにお題を通知。エセ芸術家にはジャンルのみ通知。
- **ターン制お絵かき（キャンバス機能）フェーズ**
  - `react-sketch-canvas` または `perfect-freehand` を利用したフリーハンド描画。
  - プレイヤーごとに描画色を割り当て。
  - 自分のターンのプレイヤーのみがキャンバスに「1筆」だけ描ける。マウス/指を離した瞬間に描画データを `game_events` へ送信し、同時に `rooms` のターンを次の人へ更新する。
  - 全員が2周（2筆ずつ）描いたらお絵かきフェーズ終了。
- **投票・勝敗判定フェーズ**
  - 誰がエセ芸術家かを各自が投票。最多得票者を開票。
  - エセ芸術家が最多票だった場合、エセ芸術家による「お題当て（逆転チャンス）」フェーズへ移行。
  - 最終的な勝敗結果の表示と、ポータル（ロビー）への帰還。
